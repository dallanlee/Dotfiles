ssh-keygen -t rsa -C "dallanlee@me.com"

# Copy ssh key to github.com
code ~/.ssh/id_rsa.pub

# Test connection
ssh -T git@github.com

# Set git config values
git config --global user.name "Dallan Clawson"
git config --global user.email "dallanlee@me.com"
git config --global github.user dallanlee
git config --global github.token [token_here]

git config --global core.editor "code -w"
git config --global color.ui true
